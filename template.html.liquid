<style>
  .glucose-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 1rem;
    color: #0ff;
    font-family: monospace;
  }

  .glucose-value {
    font-size: 2.5rem;
    font-weight: bold;
  }

  .glucose-trend {
    margin-top: 0.5rem;
    font-size: 1.2rem;
  }

  .glucose-time {
    margin-top: 0.5rem;
    font-size: 0.9rem;
    color: #888;
  }

  .glucose-chart {
    margin-top: 1rem;
    font-family: monospace;
    font-size: 1.3rem;
    color: #0ff;
    width: 100%;
    text-align: center;
    word-wrap: break-word;
  }
</style>

<div class="glucose-container">
  <!-- Top Section: Latest Reading -->
  <div class="glucose-value">
    🩸 {{ data.glucose }} mg/dL
  </div>

  <div class="glucose-trend">
    {{ data.trend }}
  </div>

  <div class="glucose-time">
    🕒 Last updated: {{ data.time }}
  </div>

  <!-- Bottom Section: Graph -->
  <div class="glucose-chart">
    {{ data.chart }}
  </div>
</div>
